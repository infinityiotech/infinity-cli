{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsGenerateFilesApp",
  "title": "Application Options Schema for Angular projects feat Aliased SASS",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the new AngularDash application.",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the AngularDash application?"
    }
  },
  "required": [
    "name"
  ]
}